import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Hello Node',
  mode: 'site',
  locales: [['zh-CN', '中文']],
  // ssr: {},
  // more config: https://d.umijs.org/config
});
